[project]
name = "curvadanorm"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "numpy>=1.21.0",
  "PyYAML>=5.4",
  "requests>=2.25",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
