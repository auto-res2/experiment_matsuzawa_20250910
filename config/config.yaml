# Minimal default configuration suitable for the lightweight baseline.
# The presence of at least one dataset / model entry prevents runtime
# errors due to empty dictionaries in the training script.

global:
  seeds: [1]
  mixed_precision: false
  device: "cpu"

datasets:
  dummy: {}

models:
  dummy: {}
